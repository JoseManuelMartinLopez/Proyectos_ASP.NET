@model IEnumerable<ProyectoMVC4conBD.Models.cargo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.car_des)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.car_des)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.car_cod }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.car_cod }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.car_cod })
        </td>
    </tr>
}

</table>
